// problem 01 solve

// kilometerToMeter

function kilometerToMeter(meter) {
	var km = meter * 1000; // 1 km = 1000 meter
	console.log('Check kilometer =>', km);
	return km;
}
var kMeter = kilometerToMeter(1);
console.log('Here KiloMeter is =>', kMeter);
